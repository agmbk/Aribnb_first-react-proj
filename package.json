{
  "name": "server",
  "version": "1.0.0",
  "description": "",
  "main": "server/index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "start": "node server/index.js",
    "build": "cd client && npm install && npm run build",
    "dev": "start npm run start && cd client && start npm run start"
  },
  "keywords": [],
  "author": "Toka",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.1"
  }
}
